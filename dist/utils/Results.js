class Results {
    constructor(folder, packageManager, status) {
        this.folder = folder;
        this.packageManager = packageManager;
        this.status = status;
    }
}
export { Results };
